<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/welcome_pascal_nav"
    app:startDestination="@id/greetingFragment">
    <fragment
        android:id="@+id/greetingFragment"
        android:name="com.example.pascalandroid.welcome.screen.GreetingFragment"
        android:label="fragment_greeting"
        tools:layout="@layout/fragment_greeting" />
    <activity
        android:id="@+id/authActivity"
        android:name="com.example.pascalandroid.auth.AuthActivity"
        android:label="activity_auth"
        tools:layout="@layout/activity_auth" />
    <action
        android:id="@+id/action_global_greetingFragment"
        app:destination="@id/authActivity" />
</navigation>