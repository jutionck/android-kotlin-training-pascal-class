<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/pascal_app_nav"
    app:startDestination="@id/greetingFragment">
    <action
        android:id="@+id/action_greetingFragment_to_homeActivity"
        app:destination="@id/homeActivity" />
    <fragment
        android:id="@+id/greetingFragment"
        android:name="com.example.pascalandroid.screen.GreetingFragment"
        android:label="fragment_greeting"
        tools:layout="@layout/fragment_greeting" >
    </fragment>
    <activity
        android:id="@+id/homeActivity"
        android:name="com.example.pascalandroid.activity.HomeActivity"
        android:label="activity_home"
        tools:layout="@layout/activity_home" />
</navigation>